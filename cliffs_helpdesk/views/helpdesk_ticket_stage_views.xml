<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="helpdesk_ticket_stage_view_list" model="ir.ui.view">
        <field name="name">helpdesk.ticket.stage.view.list</field>
        <field name="model">helpdesk.ticket.stage</field>
        <field name="arch" type="xml">
            <list string="Stages" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="fold"/>
                <field name="is_closed"/>
            </list>
        </field>
    </record>

    <record id="helpdesk_ticket_stage_view_form" model="ir.ui.view">
        <field name="name">helpdesk.ticket.stage.view.form</field>
        <field name="model">helpdesk.ticket.stage</field>
        <field name="arch" type="xml">
            <form string="Stage">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="fold"/>
                        <field name="is_closed"/>
                        <field name="description"/>
                        <field name="active" invisible="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="helpdesk_ticket_stage_action" model="ir.actions.act_window">
        <field name="name">Stages</field>
        <field name="res_model">helpdesk.ticket.stage</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add a Stage
            </p><p>
                Stages define the kanban columns Helpdesk Tickets move through.
            </p>
        </field>
    </record>
</odoo>
