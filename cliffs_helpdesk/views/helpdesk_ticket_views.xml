<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="helpdesk_ticket_view_list" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.list</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <list string="Tickets" default_order="priority_sequence desc, id desc" decoration-muted="is_closed">
                <field name="ticket_ref"/>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="partner_company_id"/>
                <field name="queue_id"/>
                <field name="stage_id"/>
                <field name="priority_id"/>
                <field name="ticket_type_id"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="date_deadline"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="priority_sequence" column_invisible="1"/>
                <field name="is_closed" column_invisible="1"/>
            </list>
        </field>
    </record>

    <record id="helpdesk_ticket_view_form" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <form string="Helpdesk Ticket">
                <header>
                    <field name="stage_id" widget="statusbar" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Subject"/>
                        <h1><field name="name" placeholder="e.g. Cannot access my account"/></h1>
                        <field name="ticket_ref" readonly="1" class="text-muted"/>
                    </div>
                    <group>
                        <group string="Ticket">
                            <field name="queue_id"/>
                            <field name="user_id"/>
                            <field name="priority_id"/>
                            <field name="ticket_type_id"/>
                            <field name="source_id"/>
                            <field name="tag_ids" widget="many2many_tags"/>
                            <field name="date_deadline"/>
                        </group>
                        <group string="Customer">
                            <field name="partner_id"/>
                            <field name="partner_company_id"/>
                            <field name="partner_email"/>
                            <field name="partner_phone"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Describe the issue..."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="helpdesk_ticket_view_kanban" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.kanban</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <kanban default_group_by="stage_id" class="o_kanban_small_column">
                <templates>
                    <t t-name="card">
                        <div class="p-2">
                            <strong><field name="name"/></strong>
                            <div class="text-muted">
                                <field name="ticket_ref"/> - <field name="queue_id"/>
                            </div>
                            <field name="tag_ids" widget="many2many_tags"/>
                            <div class="d-flex justify-content-between align-items-center mt-1">
                                <field name="priority_id"/>
                                <field name="user_id" widget="many2one_avatar_user"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="helpdesk_ticket_view_search" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.search</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <search string="Tickets">
                <field name="name" string="Ticket"
                       filter_domain="['|', '|', ('name', 'ilike', self), ('ticket_ref', 'ilike', self), ('partner_id', 'ilike', self)]"/>
                <field name="queue_id"/>
                <field name="partner_id"/>
                <field name="partner_company_id"/>
                <field name="user_id"/>
                <field name="tag_ids"/>
                <filter string="My Tickets" name="my_tickets" domain="[('user_id', '=', uid)]"/>
                <filter string="Unassigned" name="unassigned" domain="[('user_id', '=', False)]"/>
                <separator/>
                <filter string="Open" name="open" domain="[('is_closed', '=', False)]"/>
                <filter string="Closed" name="closed" domain="[('is_closed', '=', True)]"/>
                <separator/>
                <filter string="Overdue" name="overdue"
                        domain="[('date_deadline', '&lt;', context_today().strftime('%Y-%m-%d')), ('is_closed', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Queue" name="group_by_queue" context="{'group_by': 'queue_id'}"/>
                    <filter string="Stage" name="group_by_stage" context="{'group_by': 'stage_id'}"/>
                    <filter string="Priority" name="group_by_priority" context="{'group_by': 'priority_id'}"/>
                    <filter string="Assigned Agent" name="group_by_user" context="{'group_by': 'user_id'}"/>
                    <filter string="Type" name="group_by_type" context="{'group_by': 'ticket_type_id'}"/>
                    <filter string="Company" name="group_by_company" context="{'group_by': 'company_id'}"
                            groups="base.group_multi_company"/>
                </group>
            </search>
        </field>
    </record>

    <record id="helpdesk_ticket_action" model="ir.actions.act_window">
        <field name="name">Tickets</field>
        <field name="res_model">helpdesk.ticket</field>
        <field name="view_mode">kanban,list,form,activity</field>
        <field name="search_view_id" ref="helpdesk_ticket_view_search"/>
        <field name="context">{'search_default_open': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No tickets yet.
            </p><p>
                Tickets are created here, or automatically from emails sent to a Queue's alias.
            </p>
        </field>
    </record>
</odoo>
